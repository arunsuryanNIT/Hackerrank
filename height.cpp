#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int T;
    cin >> T;
    while(T--)
    {
        int size;
        cin >> size;
        int arr[size];
        for(int i = 0; i < size; i++)
        cin >> arr[i];
        for(int )
    }
}