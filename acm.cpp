#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int n, m, i, j;
    cin >> n >> m;
    char str[n][m];
    for(i = 0; i < n; i++)
        for(j = 0; j < m; j++)
            cin >> str[i][j];
    
}